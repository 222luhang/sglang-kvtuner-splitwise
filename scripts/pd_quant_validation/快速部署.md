# 🚀 快速部署 - POSIX 后端 KVTuner 验证

## 1️⃣ Prefill 节点 (10.60.6.75:30000)

```bash
export SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX
~/.venv/bin/python3 -m sglang.launch_server \
  --model-path /data/Qwen/Qwen2.5-7B \
  --port 30000 --tp-size 2 \
  --disaggregation-mode prefill \
  --disaggregation-transfer-backend nixl \
  --disaggregation-bootstrap-port 8998 \
  --kv-cache-dtype fp8_e5m2 \
  --enable-kvtuner-quant \
  --kvtuner-layer-config ~/sglang-config/qwen2.5-7b_layer_quant.json \
  --kvtuner-nbits-key 4 --kvtuner-nbits-value 4 \
  --mem-fraction-static 0.8 \
  --max-running-requests 256 \
  --disable-custom-all-reduce \
  > /tmp/sglang_prefill.log 2>&1 &
```

## 2️⃣ Decode 节点 (10.60.19.152:30001)

```bash
export SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX
~/.venv/bin/python3 -m sglang.launch_server \
  --model-path /data/Qwen/Qwen2.5-7B \
  --port 30001 --tp-size 2 \
  --disaggregation-mode decode \
  --disaggregation-transfer-backend nixl \
  --disaggregation-bootstrap-port 8998 \
  --kv-cache-dtype fp8_e5m2 \
  --enable-kvtuner-quant \
  --kvtuner-layer-config ~/sglang-config/qwen2.5-7b_layer_quant.json \
  --kvtuner-nbits-key 8 --kvtuner-nbits-value 8 \
  --mem-fraction-static 0.8 \
  --max-running-requests 128 \
  --disable-custom-all-reduce \
  > /tmp/sglang_decode.log 2>&1 &
```

## 3️⃣ 验证服务

```bash
# 等待 30 秒

# 检查 Prefill
curl -s http://10.60.6.75:30000/health && echo "Prefill OK"

# 检查 Decode
curl -s http://10.60.19.152:30001/health && echo "Decode OK"
```

## 4️⃣ 验证 KVTuner 配置

```bash
cd /home/ubuntu/.openclaw/workspace/sglang-kvtuner-splitwise/scripts/pd_quant_validation
python3 verify_kvtuner_quant.py
```

## 5️⃣ 运行完整测试

```bash
python3 test_complete_pd_flow.py
```

---

## 📝 关键检查点

### ✅ 层级量化配置生效

```bash
curl -s http://10.60.6.75:30000/get_server_info | python3 -c "
import sys,json
d=json.load(sys.stdin)
print('KVTuner:', d.get('enable_kvtuner_quant'))
print('Layer-wise:', d.get('enable_kvtuner_layer_wise'))
print('Config:', d.get('kvtuner_layer_config_file'))
print('K-bits:', d.get('kvtuner_nbits_key'))
print('V-bits:', d.get('kvtuner_nbits_value'))
"
```

### ✅ 显存压缩效果

- **Prefill (4-bit)**: 预期 KV Cache ~5-6 GB (压缩比 ~4x)
- **Decode (8-bit)**: 预期 KV Cache ~11-12 GB (压缩比 ~2x)

### ✅ POSIX 后端

```bash
# 检查日志中是否有 NIXL POSIX 初始化信息
grep -i "posix\|nixl" /tmp/sglang_prefill.log | tail -20
```

---

## 🔧 故障排查

```bash
# 查看日志
tail -100 /tmp/sglang_prefill.log
tail -100 /tmp/sglang_decode.log

# 停止服务
pkill -f "sglang.launch_server.*30000"
pkill -f "sglang.launch_server.*30001"

# 检查 Bootstrap
curl -s http://10.60.179.106:8998/health
```

---

**详细文档**: `POSIX_部署指南.md`  
**技术分析**: `NIXL_FIX_REPORT.md`
