# 📦 代码同步与安装指南

**问题**: 各节点 sglang 代码版本不一致  
**目标**: 同步最新代码到所有节点并重新安装

---

## 📋 准备工作

### 1. 本地打包代码

```bash
cd /home/ubuntu/.openclaw/workspace
tar -czf /tmp/sglang-kvtuner-splitwise.tar.gz \
  --exclude='.git' \
  --exclude='*.pyc' \
  --exclude='__pycache__' \
  sglang-kvtuner-splitwise

echo "代码包大小:"
ls -lh /tmp/sglang-kvtuner-splitwise.tar.gz
```

### 2. 创建层级量化配置文件

```bash
mkdir -p ~/sglang-config
cat > ~/sglang-config/qwen2.5-7b_layer_quant.json << 'EOF'
{
  "model": "Qwen2.5-7B",
  "layer_bits": {
    "0": 8, "1": 8,
    "2": 6, "3": 6,
    "4": 4, "5": 4, "6": 4, "7": 4, "8": 4,
    "9": 4, "10": 4, "11": 4, "12": 4, "13": 4,
    "14": 4, "15": 4, "16": 4, "17": 4, "18": 4,
    "19": 4, "20": 4, "21": 4, "22": 4, "23": 4,
    "24": 6, "25": 6,
    "26": 8, "27": 8
  },
  "residual_length": 256,
  "q_group_size": 64,
  "asym": false
}
EOF
```

---

## 🚀 在各节点执行

### 节点列表

| 角色 | IP | 端口 |
|------|-----|------|
| Prefill 1 | 10.60.6.75 | 30000 |
| Prefill 2 | 10.60.9.62 | 30000 |
| Decode 1 | 10.60.19.152 | 30001 |
| Decode 2 | 10.60.176.217 | 30001 |

---

### 在每个节点上执行以下步骤

#### 步骤 1: 停止旧服务

```bash
pkill -f "sglang.launch_server" || true
sleep 2
```

#### 步骤 2: 删除旧代码

```bash
rm -rf /home/ubuntu/sglang-kvtuner-adapter
```

#### 步骤 3: 上传并解压新代码

**在本地执行** (将代码包复制到各节点):
```bash
# Prefill 节点
scp /tmp/sglang-kvtuner-splitwise.tar.gz ubuntu@10.60.6.75:/tmp/
scp /tmp/sglang-kvtuner-splitwise.tar.gz ubuntu@10.60.9.62:/tmp/

# Decode 节点
scp /tmp/sglang-kvtuner-splitwise.tar.gz ubuntu@10.60.19.152:/tmp/
scp /tmp/sglang-kvtuner-splitwise.tar.gz ubuntu@10.60.176.217:/tmp/

# 配置文件
scp ~/sglang-config/qwen2.5-7b_layer_quant.json ubuntu@10.60.6.75:/home/ubuntu/sglang-config/
scp ~/sglang-config/qwen2.5-7b_layer_quant.json ubuntu@10.60.9.62:/home/ubuntu/sglang-config/
scp ~/sglang-config/qwen2.5-7b_layer_quant.json ubuntu@10.60.19.152:/home/ubuntu/sglang-config/
scp ~/sglang-config/qwen2.5-7b_layer_quant.json ubuntu@10.60.176.217:/home/ubuntu/sglang-config/
```

**在各节点执行** (解压代码):
```bash
cd /home/ubuntu
rm -rf sglang-kvtuner-splitwise
tar -xzf /tmp/sglang-kvtuner-splitwise.tar.gz
rm /tmp/sglang-kvtuner-splitwise.tar.gz
```

#### 步骤 4: 安装依赖

```bash
cd /home/ubuntu/sglang-kvtuner-splitwise
pip3 install -e python/ -q
```

#### 步骤 5: 启动服务

**Prefill 节点** (10.60.6.75, 10.60.9.62):
```bash
export SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX

cd /home/ubuntu/sglang-kvtuner-splitwise
~/.venv/bin/python3 -m sglang.launch_server \
  --model-path /data/Qwen/Qwen2.5-7B \
  --host 0.0.0.0 --port 30000 --tp-size 2 \
  --disaggregation-mode prefill \
  --disaggregation-transfer-backend nixl \
  --disaggregation-bootstrap-port 8998 \
  --kv-cache-dtype fp8_e5m2 \
  --enable-kvtuner-quant \
  --kvtuner-layer-config /home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json \
  --kvtuner-nbits-key 4 --kvtuner-nbits-value 4 \
  --kvtuner-residual-length 256 \
  --mem-fraction-static 0.8 \
  --max-running-requests 256 \
  --disable-custom-all-reduce \
  > /tmp/sglang_prefill_lq.log 2>&1 &

echo "Prefill 服务已启动"
sleep 30
curl -s http://localhost:30000/health && echo "✓ 服务就绪"
```

**Decode 节点** (10.60.19.152, 10.60.176.217):
```bash
export SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX

cd /home/ubuntu/sglang-kvtuner-splitwise
~/.venv/bin/python3 -m sglang.launch_server \
  --model-path /data/Qwen/Qwen2.5-7B \
  --host 0.0.0.0 --port 30001 --tp-size 2 \
  --disaggregation-mode decode \
  --disaggregation-transfer-backend nixl \
  --disaggregation-bootstrap-port 8998 \
  --kv-cache-dtype fp8_e5m2 \
  --enable-kvtuner-quant \
  --kvtuner-layer-config /home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json \
  --kvtuner-nbits-key 8 --kvtuner-nbits-value 8 \
  --kvtuner-residual-length 32 \
  --mem-fraction-static 0.8 \
  --max-running-requests 128 \
  --disable-custom-all-reduce \
  > /tmp/sglang_decode_lq.log 2>&1 &

echo "Decode 服务已启动"
sleep 30
curl -s http://localhost:30001/health && echo "✓ 服务就绪"
```

---

## ✅ 验证安装

### 1. 检查服务健康

```bash
curl http://10.60.6.75:30000/health
curl http://10.60.19.152:30001/health
```

### 2. 验证层级量化配置

```bash
curl -s http://10.60.6.75:30000/get_server_info | python3 << 'EOF'
import sys, json
info = json.load(sys.stdin)
print("Prefill 节点配置:")
print(f"  KVTuner 启用：{info.get('enable_kvtuner_quant')}")
print(f"  层级量化：{info.get('enable_kvtuner_layer_wise')}")
print(f"  配置文件：{info.get('kvtuner_layer_config_file')}")
print(f"  K-bits: {info.get('kvtuner_nbits_key')}")
print(f"  V-bits: {info.get('kvtuner_nbits_value')}")

layer_bits = info.get('kvtuner_layer_bits')
if layer_bits and isinstance(layer_bits, dict):
    print(f"\n层级配置 (共 {len(layer_bits)} 层):")
    for i in range(5):
        print(f"  Layer {i}: {layer_bits.get(str(i), 'N/A')}-bit")
    print("  ...")
EOF
```

### 3. 运行完整测试

```bash
cd /home/ubuntu/.openclaw/workspace/sglang-kvtuner-splitwise/scripts/pd_quant_validation
python3 test_complete_pd_flow.py
```

---

## 📊 预期结果

```
✅ 完整 P/D 分离流程测试成功!

Prefill 节点配置:
  KVTuner 启用：True
  层级量化：True
  配置文件：/home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json
  
层级配置:
  Layer 0: 8-bit
  Layer 1: 8-bit
  Layer 2: 6-bit
  Layer 3: 6-bit
  Layer 4: 4-bit
  ...

量化效果:
  压缩比：3.0-4.0x
  显存节省：60-75%
```

---

## 🔧 故障排查

### 服务启动失败
```bash
# 查看日志
tail -100 /tmp/sglang_prefill_lq.log
tail -100 /tmp/sglang_decode_lq.log
```

### 层级量化未生效
- 检查配置文件路径
- 验证 JSON 格式：`python3 -c "import json; json.load(open('/home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json'))"`

### NIXL 错误
- 确认 `export SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX`
- 检查 Bootstrap 服务：`curl http://10.60.179.106:8998/health`

---

**自动化脚本**: `sync_and_install_sglang.sh`
