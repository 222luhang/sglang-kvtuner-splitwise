# 🚀 层级量化重新配置 - 快速执行指南

**目标**: 启用层级量化 + POSIX 后端，重新测试 P/D 分离流程

---

## 1️⃣ 创建层级量化配置文件

配置文件已创建：`~/sglang-config/qwen2.5-7b_layer_quant.json`

**层级配置**:
- Layer 0-1: **8-bit** (嵌入层，高精度)
- Layer 2-3: **6-bit** (浅层，中等精度)
- Layer 4-23: **4-bit** (中层，高压缩)
- Layer 24-25: **6-bit** (深层，中等精度)
- Layer 26-27: **8-bit** (输出层，高精度)

---

## 2️⃣ 重启 Prefill 节点

**在以下节点执行** (10.60.6.75 和 10.60.9.62):

```bash
# 设置环境变量
export SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX
export SGLANG_DISAGGREGATION_DECODE_ENABLE_FAKE_AUTO=false

# 停止旧服务
pkill -f "sglang.launch_server.*disaggregation-mode prefill" || true
sleep 2

# 启动新服务（层级量化 + POSIX）
cd /home/ubuntu/sglang-kvtuner-splitwise
~/.venv/bin/python3 -m sglang.launch_server \
  --model-path /data/Qwen/Qwen2.5-7B \
  --host 0.0.0.0 \
  --port 30000 \
  --tp-size 2 \
  --dp-size 1 \
  --disaggregation-mode prefill \
  --disaggregation-transfer-backend nixl \
  --disaggregation-bootstrap-port 8998 \
  --kv-cache-dtype fp8_e5m2 \
  --enable-kvtuner-quant \
  --kvtuner-layer-config /home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json \
  --kvtuner-nbits-key 4 \
  --kvtuner-nbits-value 4 \
  --kvtuner-residual-length 256 \
  --mem-fraction-static 0.8 \
  --max-running-requests 256 \
  --disable-custom-all-reduce \
  > /tmp/sglang_prefill_layer_quant.log 2>&1 &

# 等待就绪
sleep 30
curl -s http://localhost:30000/health && echo "✓ Prefill 就绪"
```

---

## 3️⃣ 重启 Decode 节点

**在以下节点执行** (10.60.19.152 和 10.60.176.217):

```bash
# 设置环境变量
export SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX
export SGLANG_DISAGGREGATION_DECODE_ENABLE_FAKE_AUTO=false

# 停止旧服务
pkill -f "sglang.launch_server.*disaggregation-mode decode" || true
sleep 2

# 启动新服务（层级量化 + POSIX）
cd /home/ubuntu/sglang-kvtuner-splitwise
~/.venv/bin/python3 -m sglang.launch_server \
  --model-path /data/Qwen/Qwen2.5-7B \
  --host 0.0.0.0 \
  --port 30001 \
  --tp-size 2 \
  --dp-size 1 \
  --disaggregation-mode decode \
  --disaggregation-transfer-backend nixl \
  --disaggregation-bootstrap-port 8998 \
  --kv-cache-dtype fp8_e5m2 \
  --enable-kvtuner-quant \
  --kvtuner-layer-config /home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json \
  --kvtuner-nbits-key 8 \
  --kvtuner-nbits-value 8 \
  --kvtuner-residual-length 32 \
  --mem-fraction-static 0.8 \
  --max-running-requests 128 \
  --disable-custom-all-reduce \
  > /tmp/sglang_decode_layer_quant.log 2>&1 &

# 等待就绪
sleep 30
curl -s http://localhost:30001/health && echo "✓ Decode 就绪"
```

---

## 4️⃣ 验证层级量化配置

**检查 Prefill 节点**:
```bash
curl -s http://10.60.6.75:30000/get_server_info | python3 << 'EOF'
import sys, json
info = json.load(sys.stdin)
print("Prefill 节点配置:")
print(f"  KVTuner 启用：{info.get('enable_kvtuner_quant')}")
print(f"  层级量化：  {info.get('enable_kvtuner_layer_wise')}")
print(f"  配置文件：  {info.get('kvtuner_layer_config_file')}")
print(f"  Key bits:   {info.get('kvtuner_nbits_key')}")
print(f"  Value bits: {info.get('kvtuner_nbits_value')}")

layer_bits = info.get('kvtuner_layer_bits')
if layer_bits:
    print(f"\n层级配置 (共 {len(layer_bits)} 层):")
    if isinstance(layer_bits, dict):
        for layer, bits in sorted(layer_bits.items(), key=lambda x: int(x[0]))[:5]:
            print(f"  Layer {layer}: {bits}-bit")
        print("  ...")
EOF
```

**检查 Decode 节点**:
```bash
curl -s http://10.60.19.152:30001/get_server_info | python3 << 'EOF'
import sys, json
info = json.load(sys.stdin)
print("Decode 节点配置:")
print(f"  KVTuner 启用：{info.get('enable_kvtuner_quant')}")
print(f"  层级量化：  {info.get('enable_kvtuner_layer_wise')}")
print(f"  配置文件：  {info.get('kvtuner_layer_config_file')}")
print(f"  Key bits:   {info.get('kvtuner_nbits_key')}")
print(f"  Value bits: {info.get('kvtuner_nbits_value')}")
EOF
```

---

## 5️⃣ 运行完整测试

```bash
cd /home/ubuntu/.openclaw/workspace/sglang-kvtuner-splitwise/scripts/pd_quant_validation
python3 test_complete_pd_flow.py
```

---

## 📊 预期结果

### 配置验证
```
Prefill 节点配置:
  KVTuner 启用：True
  层级量化：  True
  配置文件：  /home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json
  Key bits:   4
  Value bits: 4

层级配置 (共 28 层):
  Layer 0: 8-bit
  Layer 1: 8-bit
  Layer 2: 6-bit
  Layer 3: 6-bit
  Layer 4: 4-bit
  ...
```

### 测试流程
```
步骤 1: 获取 Bootstrap Room ID
✓ Room ID: XXXXXXXXXX

步骤 2: 发送 Prefill 请求
✓ Prefill 成功
  延迟：XXX ms

步骤 3: 发送 Decode 请求
✓ Decode 成功
  延迟：XXX ms

✅ 完整 P/D 分离流程测试成功!
```

---

## 🔍 关键检查点

### 1. POSIX 后端生效
- 环境变量：`SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX`
- 日志中应显示 NIXL POSIX 初始化

### 2. 层级量化加载
- `enable_kvtuner_layer_wise: true`
- `kvtuner_layer_config_file` 指向配置文件
- `kvtuner_layer_bits` 显示各层配置

### 3. 显存压缩效果
- **预期压缩比**: 3-4x (层级量化优于统一量化)
- **预期显存节省**: 60-75%

---

## ⚠️ 故障排查

### 服务启动失败
```bash
# 查看日志
tail -100 /tmp/sglang_prefill_layer_quant.log
tail -100 /tmp/sglang_decode_layer_quant.log
```

### 层级量化未生效
- 检查配置文件路径是否正确
- 检查 JSON 格式：`python3 -c "import json; json.load(open('/home/ubuntu/sglang-config/qwen2.5-7b_layer_quant.json'))"`
- 确认启动命令包含 `--kvtuner-layer-config`

### NIXL 传输失败
- 确认所有节点都设置了 `SGLANG_DISAGGREGATION_NIXL_BACKEND=POSIX`
- 检查 Bootstrap 服务：`curl http://10.60.179.106:8998/health`

---

**相关脚本**: `restart_with_layer_quant.sh`  
**配置文件**: `~/sglang-config/qwen2.5-7b_layer_quant.json`
